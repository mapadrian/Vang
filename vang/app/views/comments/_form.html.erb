<%= simple_form_for([@picture, @picture.comments.build]) do |f| %>
  <div class="field">
    <div class="control">
      <%= f.input :response, input_html: {class: 'textarea'}, wrapper: false, label_html: {class: 'label-hidden'} %>
    </div>
  </div>
    <%= f.button :submit, 'Leave a comment', class: 'button is-primary' %>
<% end %>
  